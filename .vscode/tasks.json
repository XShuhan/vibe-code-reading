{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "code-vibe: build",
      "type": "shell",
      "command": "${env:APPDATA}\\npm\\pnpm.cmd",
      "args": [
        "build"
      ],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "Path": "C:\\Program Files\\nodejs;${env:APPDATA}\\npm;${env:Path}"
        }
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$tsc"
      ]
    }
  ]
}
